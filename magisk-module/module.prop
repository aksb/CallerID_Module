id=callerid_keepalive
name=CallerIDModule 保活助手
version=v4.2
versionCode=2
author=you
description=不包含任何 APK，纯脚本模块：开机自动给「来电识别」(com.callerid.module) 打开电池优化白名单/后台运行等 appops 保活开关，效果等同于手动敲 adb/root 命令，每一项执行结果都会用中文直接显示（已启用✔/未启用✘）。没装这个包名的 App 时脚本自动跳过、不做任何事；卸载这个模块也不会影响已安装的 App，只是不再开机自动保活。Magisk App 里这个模块的 Action 按钮可以立即重新执行一次并直接看到中文结果——注意该按钮需要刷入本模块并重启一次之后才会出现，这是 Magisk 本身的机制（模块要先被挂载生效才有 Action 入口），首次刷入还没重启时看不到属于正常现象；按钮出现之后，点击执行本身不需要再重启。
