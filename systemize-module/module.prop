id=callerid_systemize
name=CallerIDModule 系统化增强（可选，风险自负）
version=v4.3
versionCode=1
author=you
description=可选的增强模块，跟保活脚本模块（callerid_keepalive）完全独立，装不装、先后顺序都互不影响。作用：把「来电识别」(com.callerid.module) 变成系统应用，从而让 App 清单里的 android:persistent="true" 生效，缓解低内存机型上常驻进程被系统按内存压力回收的情况。v4.3 起改为内置占位 APK（跟正式发布的 APK 同一套签名）的方案，不再需要"先装 App 再刷模块"这个安装顺序限制，也不再需要"现场卸载已装版本"这一步——旧版这一步在实测中会导致图标丢失、App 闪退、且后续无法覆盖安装，v4.3 已经修复。
